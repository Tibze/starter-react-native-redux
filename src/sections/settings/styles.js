import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'rgb(128,115,141)',
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
  	color: 'black'
  },
  buttonContainer: {
  	marginTop: 10
  }
});

export default styles;
